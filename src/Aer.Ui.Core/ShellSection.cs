namespace Aer.Ui.Core;

/// <summary>The shell's destinations — docs/ux/information-architecture.md's flat navigation: Home, Task, Author (M19 Phase 2, #187), plus Remote (M21 Phase 3, #234), always reachable.</summary>
public enum ShellSection
{
    Home,
    Task,
    Author,
    Remote,
}
